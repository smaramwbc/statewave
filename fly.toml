app = 'statewave-api'
primary_region = 'ord'

[build]

[env]
  PORT = "8100"

[http_service]
  internal_port = 8100
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
